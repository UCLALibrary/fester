#
# Testing properties configuration values are fed by Maven POM properties, which can be set at runtime in the system
# settings.xml file (often at /etc/maven/settings.xml).
#

# S3 configuration options
manifeststore.s3.bucket=${manifeststore.s3.bucket}
manifeststore.s3.access_key=${manifeststore.s3.access_key}
manifeststore.s3.secret_key=${manifeststore.s3.secret_key}
manifeststore.s3.region=${manifeststore.s3.region}
