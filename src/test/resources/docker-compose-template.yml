version: "3.7"

services:
  {}:
    image: ${project.artifactId}:${project.version}
    ports:
      - "{}:{}"
    environment:
      - FESTER_S3_BUCKET=${fester.s3.bucket}
      - FESTER_S3_ACCESS_KEY={}
      - FESTER_S3_SECRET_KEY={}
      - FESTER_S3_REGION={}
      - FESTER_S3_ENDPOINT={}
      - IIIF_BASE_URL=${iiif.base.url}
      - FESTER_HTTP_PORT={}
